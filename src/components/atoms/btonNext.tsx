import React, { useState } from 'react'
import { type } from 'os'

function BtnNext() {
  return (
    <a className="btn-medium-next nextPag" href="#">
      Continuar
    </a>
  )
}

export default BtnNext
